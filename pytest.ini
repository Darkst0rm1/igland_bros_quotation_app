[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
filterwarnings =
    ignore::DeprecationWarning:streamlit.*
# Streamlit's AppTest harness runs the script outside a real server, which logs
# a "missing ScriptRunContext" warning per run. It is expected and harmless.
log_cli_level = ERROR
